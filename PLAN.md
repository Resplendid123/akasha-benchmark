1.数据集层，下载-校验。进行下载任务。
2.归一化层，归一化-校验；入sqlite、不入Akasha库。进行归一化任务。
3.编译层，抽子集-入库（Akasha）；根据（一个或者多个）dataset抽sample和corpus作为这次编译的子集，这一次run_id固化配置；这里把自己入Akasha库，space随机创建一个，作为这次编译的空间。进行编译任务。
4.查询层，选取某次编译的空间，配置查询参数（选取该编译的数据集中的几个QA、模型配置来做query），进行查询任务，
5.评测层，针对某次查询结果，配置评估参数（包括模型配置），进行评估任务。
6.归因层，针对某次编译->查询->评测链路（包括归因模型配置），进行归因任务。
7.任务：实时观测上述6层的任务
8.配置：所有关于Akasha连接、llm配置
9.测试：轻量的一次完整6个链路

项目采取前后端分离，所有操作都在前端请求，后端完成，项目代码中不要预留脚本或者命令行入口。
当前sql需要重构，只保留必要字段，保持架构简洁合理，重构时，不用考虑保留数据库中的数据，直接保留最终sql。
每个阶段任务都需要可暂停、可继续、清理（包括数据库相关内容）。操作日志统一用作审计（不删除）。
test文件清理，只保留必要的test。

