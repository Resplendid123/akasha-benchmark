# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Raw datasets. narrativeqa inlines Project Gutenberg books and IMSDb
# screenplays that must not be redistributed.
/dataset/

# Evaluation artifacts (normalized data, responses, reports).
/data/

# 评测库。**这是事实来源**（docs/PLAN.md §12 决策 2），不是缓存 ——
# 所以它不在清理范围里，要删得显式来。
# 它同时是**凭据文件**：app_config 表存明文 Akasha 密码与 model_provider 的
# api_key（配置要能在 UI 里填改，环境变量做不到）。
/akasha_bench.db
/akasha_bench.db-wal
/akasha_bench.db-shm
# 迁移前的自动备份。
/akasha_bench.db.pre-*

# 前端构建产物与依赖。
/web/dist/
/web/node_modules/

# 平台的任务日志。
/logs/

# Test and tooling caches
.pytest_cache/
.ruff_cache/
.idea
*.log
.tmptest/
CLAUDE.md